










read over /docs and make sure all changes are in it too

hardcore debugging

**change links in the code**





ideas:
get a lawer to confirm the plausible deniability
exe that installs web server and password bolt and sets it up -> practice for win32 c++
	tell them to download and install xampp, point the program to it's folder
	it will configure windows firewall and xampp settings for security based on their needs
	//shows ip of their router and link to it so they can open ports on it if necessary
	//check that ip of gateway is a LAN addr before doing anything with it
	//security warnings about opening ports





remove the random double hash thing
